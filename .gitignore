node_modules/
.next/
.vercel/
.env.local
.vercel
".env.local"
.env*

# Application distincte (valorimmo.app), possède son propre dépôt Git —
# ne doit jamais être suivie dans le dépôt closia.
valorimmo/

# Cache TypeScript, ne doit pas être versionné
tsconfig.tsbuildinfo
